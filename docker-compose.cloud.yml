services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: neuramemory-server
    restart: unless-stopped
    env_file:
      - ./server/.env.cloud
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    ports:
      - "3000:3000"
    networks:
      - neuramemory-network
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          memory: 512M

  postgres:
    image: postgres:16-alpine
    container_name: neuramemory-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: neuramemory
      POSTGRES_PASSWORD: neuramemory
      POSTGRES_DB: neuramemory
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./server/migrations/001_init.sql:/docker-entrypoint-initdb.d/001_init.sql:ro
    networks:
      - neuramemory-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U neuramemory" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.3'
          memory: 512M

  qdrant:
    image: qdrant/qdrant:latest
    container_name: neuramemory-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - neuramemory-network
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: 2560M
        reservations:
          memory: 512M

  crawl4ai:
    image: unclecode/crawl4ai:latest
    container_name: neuramemory-crawl4ai
    restart: unless-stopped
    shm_size: '1gb'
    networks:
      - neuramemory-network
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          memory: 1G

volumes:
  postgres_data:
    driver: local
  qdrant_data:
    driver: local

networks:
  neuramemory-network:
    driver: bridge
